export enum ESizes {
  SMALL = 'small',
  MEDIUM = 'medium',
  BIG = 'big',
}

export enum EVariants {
  TEXT = 'text',
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
}

export enum EColors {
  PRIMARY = 'primary',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum EBadgeVariants {
  GRAY = 'gray',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum EBadgeShape {
  SQUARE = 'square',
  ROUND = 'round',
}
