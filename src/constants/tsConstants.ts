export enum ESizes {
  SMALL = 'small',
  MEDIUM = 'medium',
  BIG = 'big',
}

export enum EVariants {
  TEXT = 'text',
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
}

export enum EColors {
  PRIMARY = 'primary',
  WARNING = 'warning',
  ERROR = 'error',
}
